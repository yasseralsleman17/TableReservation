<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tablereservation">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />







    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TableReservation"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".Test"
            android:exported="true" />
        <activity
            android:name=".admin.Reservations"
            android:exported="true" />
        <activity
            android:name=".restaurant.RestaurantChat"
            android:exported="true" />
        <activity
            android:name=".restaurant.AddNewTable"
            android:exported="true" />
        <activity
            android:name=".admin.ViewRestaurants"
            android:exported="true" />
        <activity
            android:name=".admin.ViewCustomers"
            android:exported="true" />
        <activity
            android:name=".admin.RestaurantRegistration"
            android:exported="true" />
        <activity
            android:name=".LogIn"
            android:exported="true" />
        <activity
            android:name=".admin.CustomerRegistaration"
            android:exported="true" />
        <activity
            android:name=".admin.AdminHomePage"
            android:exported="true" />
        <activity
            android:name=".restaurant.RestaurantHomePage"
            android:exported="true" />
        <activity
            android:name=".customer.RestaurantDetails"
            android:exported="true" />
        <activity
            android:name=".customer.BrowseRestaurant"
            android:exported="true" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <activity
            android:name=".customer.MapsActivity"
            android:exported="true"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".customer.ReservationList"
            android:exported="true" />
        <activity
            android:name=".RegisterRestaurant"
            android:exported="true" />
        <activity
            android:name=".RegisterCustomer"
            android:exported="true" />
        <activity
            android:name=".LogIn"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>